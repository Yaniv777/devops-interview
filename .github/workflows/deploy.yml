on:
  workflow_dispatch:
    inputs:
      site_name:
        description: 'name of the site'
        required: false
        type: string

jobs:
  ansible-configuration:
    runs-on: ubuntu-latest
    steps:
    - run: echo 'test'